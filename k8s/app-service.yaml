apiVersion: v1
kind: Service
metadata:
  name: travel-app-service
  namespace: travel-app
spec:
  type: NodePort
  selector:
    app: travel-app
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30007